{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://wisher.app/wishlist.schema.json",
  "title": "Wisher Wishlist Document",
  "description": "Canonical schema for Wisher wishlist import/export documents. Images are stored inline as base64 data URLs.",
  "type": "object",
  "required": ["schemaVersion", "lists"],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Version of this schema. Must be 1."
    },
    "lists": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/wishlist"
      }
    }
  },
  "definitions": {
    "wishlist": {
      "type": "object",
      "required": ["title"],
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Wishlist title."
        },
        "description": {
          "type": "string",
          "description": "Optional wishlist description."
        },
        "eventDate": {
          "type": "integer",
          "description": "Optional event date as a Unix timestamp (milliseconds)."
        },
        "ordered": {
          "type": "boolean",
          "description": "Whether this is a manually-ordered list."
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/item"
          }
        }
      }
    },
    "item": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Item name."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Optional product URL."
        },
        "priceMinor": {
          "type": "integer",
          "minimum": 0,
          "description": "Price in minor currency units (e.g. cents)."
        },
        "currency": {
          "type": "string",
          "default": "USD",
          "description": "ISO 4217 currency code, defaults to USD."
        },
        "image": {
          "type": "string",
          "description": "Optional image as a base64 data URL (data:image/...;base64,...)."
        },
        "notes": {
          "type": "string",
          "description": "Optional free-form notes."
        },
        "rank": {
          "type": "integer",
          "minimum": 0,
          "description": "Manual order position, used for ordered lists."
        },
        "priority": {
          "type": "string",
          "enum": ["low", "medium", "high"],
          "default": "medium",
          "description": "Item priority, defaults to medium."
        },
        "purchased": {
          "type": "boolean",
          "default": false,
          "description": "Whether the item has been purchased."
        }
      }
    }
  }
}
